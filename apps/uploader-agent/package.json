{
  "name": "@youtubbot/uploader-agent",
  "version": "1.0.0",
  "description": "Local agent that polls Vercel API and uploads to YouTube via Puppeteer Stealth",
  "main": "index.js",
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "start": "node index.js",
    "login": "node index.js --manual-login",
    "login:native": "node -e \"const cp = require('child_process'); const p = require('path').resolve(__dirname, '../../login_youtube.bat'); cp.spawn('cmd.exe', ['/c', p], { detached: true, stdio: 'ignore' });\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "puppeteer": "^22.12.1",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2"
  }
}
